'use client'
import { GroupWithEvent } from "../types/group"

const GroupsClient = ({ groupsWithEvents }: { groupsWithEvents: Array<GroupWithEvent> }) => {
    return (
        <div className="w-full flex flex-col">

        </div>
    )
}